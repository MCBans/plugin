name: smoke-test

# Boots a real server of each platform with the built jar and asserts the plugin loads/enables
# (scripts/smoke-test.sh). Heavy (downloads servers + Minecraft), so it runs on demand and weekly
# rather than on every push.
on:
  workflow_dispatch:
  schedule:
    - cron: '0 6 * * 1'   # Mondays 06:00 UTC

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        platform: [bukkit, spigot, bungeecord, sponge, forge]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Cache ForgeGradle
        if: matrix.platform == 'forge'
        uses: actions/cache@v4
        with:
          path: ~/.gradle/caches/forge_gradle
          key: forgegradle-${{ runner.os }}-1.20.1-47.2.0

      - name: Build the ${{ matrix.platform }} jar
        run: ./gradlew :${{ matrix.platform }}:build --no-daemon --stacktrace

      - name: Boot ${{ matrix.platform }} and verify the plugin enables
        run: ./gradlew --stop; bash scripts/smoke-test.sh ${{ matrix.platform }}

      - name: Upload server log on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: smoke-${{ matrix.platform }}-log
          path: .servertest/${{ matrix.platform }}/server.log
          if-no-files-found: ignore

  e2e:
    # Full end-to-end: a real Paper/Spigot server + the plugin talking to a protocol-compatible
    # /plugin/ws mock, driven through the server console (scripts/e2e-test.sh).
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build the Spigot jar
        run: ./gradlew :spigot:build --no-daemon --stacktrace

      - name: Run the end-to-end round trip
        run: ./gradlew --stop; bash scripts/e2e-test.sh

      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-logs
          path: |
            .servertest/e2e/server.log
            .servertest/e2e/frames.log
            .servertest/e2e/mock.log
          if-no-files-found: ignore
